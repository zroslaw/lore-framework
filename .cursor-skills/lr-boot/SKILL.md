---
name: lr-boot
description: "Boot a specific lore agent when you already know which agent you want. Usage: /lr-boot <agent-name>"
argument-hint: "<agent-name>"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `.cursor-skills/lr-boot/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/agent-boot.md` and boot as agent `$ARGUMENTS`.
