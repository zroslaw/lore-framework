---
name: lr-unregister-agent
description: "Remove a direct boot shortcut for one specific lore agent. Usage: /lr-unregister-agent [<lore-agent-repo>] <agent-name>"
argument-hint: "[<lore-agent-repo>] <agent-name>"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `.cursor-skills/lr-unregister-agent/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/register-repo.md` for the Unregister Agent instructions, then unregister the agent shortcut: $ARGUMENTS
