---
name: lr-register-repo
description: "Create or refresh direct boot shortcuts for every lore agent in a repo. Usage: /lr-register-repo <lore-agent-repo>"
argument-hint: "<lore-agent-repo>"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `.cursor-skills/lr-register-repo/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/register-repo.md` for detailed instructions, then register the repo: $ARGUMENTS
