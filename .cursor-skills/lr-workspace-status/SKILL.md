---
name: lr-workspace-status
description: "Diagnose the workspace — git state, descriptor drift, memory-file contract, child-repo hygiene. Read-only; every finding names the command that fixes it."
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `.cursor-skills/lr-workspace-status/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/workspace-status.md` and follow it against the current working directory.
