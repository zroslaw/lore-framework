---
name: lr-dialogue
description: "Dialogue mode: talk with me in short conversational turns — the one essential thing now, details on demand, no long articles. Keep my context in sync as we go. Say 'dialogue' to re-assert. Usage: /lr-dialogue"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `.cursor-skills/lr-dialogue/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/dialogue.md` and follow the guidance there.
