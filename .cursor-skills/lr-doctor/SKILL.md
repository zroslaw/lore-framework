---
name: lr-doctor
description: "Diagnose and heal framework runtime issues. Describe the symptom and `/lr-doctor` matches it against a catalog of known ailments and applies the fix."
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `.cursor-skills/lr-doctor/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/doctor.md` for the diagnostic procedure and the full catalog of known ailments.
