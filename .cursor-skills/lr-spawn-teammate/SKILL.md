---
name: lr-spawn-teammate
description: "BETA — Spawn lore agents as Agent Teams teammates. Usage: /lr-spawn-teammate [<agent-name> ...]"
argument-hint: "[<agent-name> ...]"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `.cursor-skills/lr-spawn-teammate/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/spawn-teammate.md` and follow the procedure there. Optional teammate agent names: `$ARGUMENTS`.
