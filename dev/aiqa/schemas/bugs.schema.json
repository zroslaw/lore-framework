{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ULA bugs.yaml — potential issues for one unit",
  "type": "object",
  "required": ["unit", "signature", "bugs"],
  "additionalProperties": false,
  "properties": {
    "unit": { "type": "string", "description": "Unit slug (= unit directory name)." },
    "signature": { "type": "string", "description": "Full unit signature." },
    "bugs": {
      "type": "array",
      "description": "Preliminary list of potential issues. May be empty.",
      "items": {
        "type": "object",
        "required": ["id", "title", "description"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
            "description": "Slug from the bug's essence, unique within the unit."
          },
          "title": {
            "type": "string",
            "description": "Short: what the bug is and how it impacts us."
          },
          "description": {
            "type": "string",
            "description": "Detailed: how to reproduce, components involved — enough to understand and reproduce."
          }
        }
      }
    }
  }
}
