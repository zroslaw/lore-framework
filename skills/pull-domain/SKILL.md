---
description: "Pull all git repos in the domain directory in parallel."
---

Run `${CLAUDE_PLUGIN_ROOT}/scripts/pull-domain` passing the current working directory as the argument. Stream its output directly to the user.
