---
description: "BETA — Initialize the AIQA sibling repo (<repo>-aiqa) for a source repository: scaffolds config and the ula/ tree. Asks the user before creating anything. Usage: /lr:dev-aiqa-repo-init [<repo>]"
argument-hint: "[<repo>]"
---

Read `${CLAUDE_PLUGIN_ROOT}/dev/aiqa/repo-init.md` and follow the procedure there. Source repo (default: current working directory): `$ARGUMENTS`.
