---
description: "BETA — Run a ULA (unit-level analysis) single-file pass: split a file into units, then per unit find potential bugs, generate clean-room test scenarios, and gap-analyse against existing tests. Persists per-file artifacts into <repo>-df. Usage: /lr:df-ula-file <file>"
argument-hint: "<file>"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `skills/df-ula-file/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/df/aiqa/ula-file.md` and follow the procedure there for the target file: `$ARGUMENTS`.
