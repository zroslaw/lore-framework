---
description: "Consult another lore agent with a short question. A subagent boots the consultant, answers from its lore, and hands back specific file pointers. Usage: /lr:consult <agent-name> [hint]"
argument-hint: "<agent-name> [hint]"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `skills/consult/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/consult.md` and follow the procedure there. Arguments (consultant agent name + optional hint): `$ARGUMENTS`.
