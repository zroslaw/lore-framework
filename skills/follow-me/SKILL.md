---
description: "Follow-me mode: you drive the thinking direction and I follow — small suggestions only, no racing ahead or re-architecting. Say 'follow me' to re-assert. Usage: /lr:follow-me"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `skills/follow-me/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/follow-me.md` and follow the guidance there.
