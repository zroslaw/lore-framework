---
description: "Recall lore topics relevant to the current session. Optional hint focuses the search. Usage: /lr:recall [hint]"
argument-hint: "[hint]"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `skills/recall/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/recall.md` and follow the procedure there. Optional hint from the user: `$ARGUMENTS`.
