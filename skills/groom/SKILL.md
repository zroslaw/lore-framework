---
description: "Iteratively improve Lore structure, retrieval efficiency, and prose quality. Usage: /lr:groom [<area-or-topic>] [--dry-run] [--all]"
argument-hint: "[<area-or-topic>] [--dry-run] [--all]"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `skills/groom/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/groom.md` and groom the current agent's Lore using `$ARGUMENTS`.
