---
name: lr-update
description: "Update domain artifacts to match the installed framework version. Usage: /lr-update [--dry-run]"
argument-hint: "[--dry-run]"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, three levels up from this `skills/cursor/lr-update/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/update.md` and execute the update process. Arguments: $ARGUMENTS
