---
name: lr-create-agent
description: "Add a new agent to an agent repo. Usage: /lr-create-agent [agent-name]"
argument-hint: "[agent-name]"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, three levels up from this `skills/cursor/lr-create-agent/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/create-agent.md` for detailed instructions, then create the agent. $ARGUMENTS
