---
name: lr-attach
description: "Attach another lore agent's knowledge to the host session for sustained co-work, or list attached guests when called with no args. Usage: /lr-attach [agent-name]"
argument-hint: "[agent-name]"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, three levels up from this `skills/cursor/lr-attach/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/attach.md` and follow the procedure there. Argument (guest agent name, optional — omit to list attached guests): `$ARGUMENTS`.
