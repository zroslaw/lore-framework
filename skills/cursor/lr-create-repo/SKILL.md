---
name: lr-create-repo
description: "Scaffold a new agent repository. Usage: /lr-create-repo <lore-agent-repo>"
argument-hint: "<lore-agent-repo>"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, three levels up from this `skills/cursor/lr-create-repo/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/create-repo.md` for detailed instructions, then create the repo: $ARGUMENTS
