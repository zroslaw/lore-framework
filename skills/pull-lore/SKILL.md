---
description: "Mid-session refresh of just the active agents' lore agent repos (use /lr:workspace-pull for full-workspace refresh). Pulls each repo and re-loads role + lore-context."
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `skills/pull-lore/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/pull-lore.md` and execute it.
