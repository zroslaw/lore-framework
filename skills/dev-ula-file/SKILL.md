---
description: "BETA — Run a ULA (unit-level analysis) single-file pass: split a file into units, then per unit find potential bugs, generate clean-room test scenarios, and gap-analyse against existing tests. Usage: /lr:dev-ula-file <file>"
argument-hint: "<file>"
---

Read `${CLAUDE_PLUGIN_ROOT}/dev/aiqa/ula-file.md` and follow the procedure there for the target file: `$ARGUMENTS`.
