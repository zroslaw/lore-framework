---
description: "BETA — Initialize the DF backbone repo (<repo>-df) for a source repository: scaffolds config and the repo-lore/ tree. Asks the user before creating anything. Usage: /lr:df-repo-init [<repo>]"
argument-hint: "[<repo>]"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `skills/df-repo-init/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/df/repo-init.md` and follow the procedure there. Source repo (default: current working directory): `$ARGUMENTS`.
