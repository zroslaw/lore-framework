---
description: "Create or refresh a direct boot shortcut for one specific lore agent. Usage: /lr:register-agent [<lore-agent-repo>] <agent-name>"
argument-hint: "[<lore-agent-repo>] <agent-name>"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `skills/register-agent/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/register-repo.md` for the Register Agent instructions, then register the agent shortcut: $ARGUMENTS
