---
description: "Switch me into plain, simple English: short clear sentences, one idea at a time, brief but not compressed. Invoke again any time I slip into dense prose. Usage: /lr:plain-language"
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `skills/plain-language/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/plain-language.md` and follow the guidance there.
