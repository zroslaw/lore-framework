---
description: "Publish workspace-level state — commit framework-managed workspace files (descriptor, memory file, gitignore, README, registered shortcuts) and push the workspace repo."
---

`<framework-root>` is the framework root — the directory that contains the `VERSION` file, two levels up from this `skills/workspace-push/SKILL.md`. Resolve it to an absolute path before using it below.

Read `<framework-root>/docs/workspace-push.md` and follow it.
